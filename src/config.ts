import { ISize } from "./types";

export const sizes: Record<string, ISize> = {
  small: { width: "400px", height: "400px" },
  wide: { width: "800px", height: "400px" },
  large: { width: "800px", height: "800px" },
};
